
# Build exe (legacy sample)
python -m PyInstaller --noconsole --onefile --name ShopManager --collect-all uvicorn --collect-all pydantic --collect-all fastapi --hidden-import=uvicorn --hidden-import=sqlalchemy.dialects.sqlite run_app.py

# -----------------------------------------------------------------------------
# Git commands: push server-repo only (production backend) - run from repo root
# -----------------------------------------------------------------------------
cd server-repo
git status
# inspect remote (optional)
git remote -v
# stage, commit and push to origin/main (adjust branch name if different)
git add .
git commit -m "deploy: update backend (production)"
git push origin main

# If server-repo has a different remote name or needs to be added:
# git remote add origin <server-repo-git-url>
# git push -u origin main

# -----------------------------------------------------------------------------
# Git commands: push whole project (main repository) - run from repo root
# -----------------------------------------------------------------------------
cd ..  # ensure you are in repository root if inside server-repo
git status
git add .
git commit -m "chore: remove legacy PyQt6, move excel tasks to backend/excel_tasks, update requirements"
git push origin main

# Optional: create a review branch and open PR
# git checkout -b cleanup/remove-pyqt6
# git add . && git commit -m "refactor: remove legacy desktop UI" && git push -u origin cleanup/remove-pyqt6



